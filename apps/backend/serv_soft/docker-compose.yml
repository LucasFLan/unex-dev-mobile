version: '3.8'
services:
  servsoft_db:
    image: bitnami/postgresql:15
    container_name: servsoft_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: servsoft
    ports:
      - "5432:5432"
    volumes:
      - servsoft_db_data:/var/lib/postgresql/data

volumes:
  servsoft_db_data:
